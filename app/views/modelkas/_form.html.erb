<%= form_with(model: modelka) do |form| %>
  <% if modelka.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modelka.errors.count, "error") %> prohibited this modelka from being saved:</h2>

      <ul>
        <% modelka.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
